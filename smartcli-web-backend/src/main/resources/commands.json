{
  "commands": [
    { "category": "kubectl", "template": "kubectl get pods -n <namespace>", "description": "List pods in a namespace" },
    { "category": "kubectl", "template": "kubectl get pods -n <namespace> -l <label-selector>", "description": "List pods filtered by label selector (e.g. app=my-app)" },
    { "category": "kubectl", "template": "kubectl get pods -n <namespace> -o wide", "description": "List pods with node and IP info" },
    { "category": "kubectl", "template": "kubectl get all -n <namespace>", "description": "List all resources in a namespace" },
    { "category": "kubectl", "template": "kubectl get deployments -n <namespace>", "description": "List deployments" },
    { "category": "kubectl", "template": "kubectl get svc -n <namespace>", "description": "List services" },
    { "category": "kubectl", "template": "kubectl get configmap -n <namespace>", "description": "List configmaps" },
    { "category": "kubectl", "template": "kubectl get secrets -n <namespace>", "description": "List secrets" },
    { "category": "kubectl", "template": "kubectl get namespaces", "description": "List namespaces" },
    { "category": "kubectl", "template": "kubectl get nodes -o wide", "description": "List nodes with extra info" },
    { "category": "kubectl", "template": "kubectl get events -n <namespace> --sort-by=.lastTimestamp", "description": "List events sorted by time" },
    { "category": "kubectl", "template": "kubectl get kafka -n <namespace>", "description": "List Kafka clusters (operator CRD)" },
    { "category": "kubectl", "template": "kubectl get kafkatopics -n <namespace>", "description": "List Kafka topics (operator CRD)" },
    { "category": "kubectl", "template": "kubectl get kafkabridge -n <namespace>", "description": "List Kafka bridges" },
    { "category": "kubectl", "template": "kubectl get kafkanodepool -n <namespace>", "description": "List Kafka node pools" },
    { "category": "kubectl", "template": "kubectl get tidbcluster -n <namespace>", "description": "List TiDB clusters" },
    { "category": "kubectl", "template": "kubectl get <resource-type> <name> -n <namespace> -o yaml", "description": "Get resource definition as YAML" },
    { "category": "kubectl", "template": "kubectl get secret <name> -n <namespace> -o jsonpath={.data.<key>}", "description": "Get a single secret value (base64)" },
    { "category": "kubectl", "template": "kubectl describe pod <pod-name> -n <namespace>", "description": "Show detailed pod info / events / env" },
    { "category": "kubectl", "template": "kubectl describe configmap <name> -n <namespace>", "description": "Describe a configmap" },
    { "category": "kubectl", "template": "kubectl describe node <node-name>", "description": "Describe a node" },
    { "category": "kubectl", "template": "kubectl describe kafka <cluster-name> -n <namespace>", "description": "Describe a Kafka cluster" },
    { "category": "kubectl", "template": "kubectl describe tidbcluster <cluster-name> -n <namespace>", "description": "Describe a TiDB cluster" },
    { "category": "kubectl", "template": "kubectl logs <pod-name> -n <namespace>", "description": "Get pod logs" },
    { "category": "kubectl", "template": "kubectl logs <pod-name> -n <namespace> --follow", "description": "Tail pod logs" },
    { "category": "kubectl", "template": "kubectl logs <pod-name> -n <namespace> --tail=<lines>", "description": "Get the last N lines of pod logs" },
    { "category": "kubectl", "template": "kubectl logs -l <label-selector> -n <namespace>", "description": "Logs from all pods matching a label" },
    { "category": "kubectl", "template": "kubectl exec -it <pod-name> -n <namespace> -- /bin/bash", "description": "Open a shell inside a pod" },
    { "category": "kubectl", "template": "kubectl exec -it <pod-name> -n <namespace> -- env", "description": "Print env vars inside a pod" },
    { "category": "kubectl", "template": "kubectl exec -it <pod-name> -n <namespace> -- cat <path-in-pod>", "description": "Cat a file inside a pod" },
    { "category": "kubectl", "template": "kubectl cp <namespace>/<pod-name>:<path-in-pod> <local-path>", "description": "Copy file from a pod to local" },
    { "category": "kubectl", "template": "kubectl cp <local-path> <namespace>/<pod-name>:<path-in-pod>", "description": "Copy file from local into a pod" },
    { "category": "kubectl", "template": "kubectl delete pod <pod-name> -n <namespace>", "description": "Delete a pod (will restart if managed)" },
    { "category": "kubectl", "template": "kubectl delete kafkatopic <topic-name> -n <namespace>", "description": "Delete a Kafka topic" },
    { "category": "kubectl", "template": "kubectl run <pod-name> -n <namespace> --image=<image> --restart=Never -- sleep infinity", "description": "Spin up a temporary pod for debugging" },
    { "category": "kubectl", "template": "kubectl scale deployment <name> -n <namespace> --replicas=<count>", "description": "Scale a deployment" },
    { "category": "kubectl", "template": "kubectl rollout restart deployment <name> -n <namespace>", "description": "Rolling restart of a deployment" },
    { "category": "kubectl", "template": "kubectl rollout status deployment <name> -n <namespace>", "description": "Check rollout status" },
    { "category": "kubectl", "template": "kubectl port-forward svc/<service-name> <local-port>:<remote-port> -n <namespace>", "description": "Port-forward to a service" },
    { "category": "kubectl", "template": "kubectl port-forward pod/<pod-name> <local-port>:<remote-port> -n <namespace>", "description": "Port-forward to a pod" },
    { "category": "kubectl", "template": "kubectl apply -f <file-path>", "description": "Apply a YAML manifest" },
    { "category": "kubectl", "template": "kubectl delete -f <file-path>", "description": "Delete resources from a YAML manifest" },
    { "category": "kubectl", "template": "kubectl top pods -n <namespace>", "description": "Show pod CPU/memory usage" },
    { "category": "kubectl", "template": "kubectl top nodes", "description": "Show node CPU/memory usage" },
    { "category": "kubectl", "template": "kubectl cordon <node-name>", "description": "Mark a node unschedulable" },
    { "category": "kubectl", "template": "kubectl uncordon <node-name>", "description": "Re-enable scheduling on a node" },
    { "category": "kubectl", "template": "kubectl drain <node-name> --ignore-daemonsets --delete-emptydir-data", "description": "Drain a node for maintenance" },
    { "category": "kubectl", "template": "kubectl config get-contexts", "description": "List kube contexts" },
    { "category": "kubectl", "template": "kubectl config use-context <context-name>", "description": "Switch context (cluster)" },
    { "category": "kubectl", "template": "kubectl config current-context", "description": "Show current context" },
    { "category": "kubectl", "template": "kubectl config set-context --current --namespace=<namespace>", "description": "Set default namespace for current context" },
    { "category": "kubectl", "template": "kubectl wait --for=condition=Ready pod/<pod-name> -n <namespace> --timeout=<duration>", "description": "Wait for a pod to be ready" },
    { "category": "kubectl", "template": "export KUBECONFIG=<path-to-kubeconfig>", "description": "Use a specific kubeconfig file" },

    { "category": "docker", "template": "docker pull <image>:<tag>", "description": "Pull an image" },
    { "category": "docker", "template": "docker images", "description": "List local images" },
    { "category": "docker", "template": "docker tag <source-image>:<source-tag> <target-image>:<target-tag>", "description": "Retag an image" },
    { "category": "docker", "template": "docker push <image>:<tag>", "description": "Push an image to a registry" },
    { "category": "docker", "template": "docker rmi <image>:<tag>", "description": "Remove a local image" },
    { "category": "docker", "template": "docker build -t <image>:<tag> .", "description": "Build an image from current directory" },
    { "category": "docker", "template": "docker build -t <image>:<tag> -f <Dockerfile-path> .", "description": "Build using a specific Dockerfile" },
    { "category": "docker", "template": "docker login <registry-url>", "description": "Log in to a Docker registry" },
    { "category": "docker", "template": "docker login <registry-url> -u <username> -p <password>", "description": "Log in to a registry with credentials" },
    { "category": "docker", "template": "docker logout <registry-url>", "description": "Log out of a registry" },
    { "category": "docker", "template": "docker run -d --name <container> -p <host-port>:<container-port> <image>:<tag>", "description": "Run a container in the background" },
    { "category": "docker", "template": "docker ps", "description": "List running containers" },
    { "category": "docker", "template": "docker ps -a", "description": "List all containers (including stopped)" },
    { "category": "docker", "template": "docker stop <container>", "description": "Stop a container" },
    { "category": "docker", "template": "docker start <container>", "description": "Start a container" },
    { "category": "docker", "template": "docker restart <container>", "description": "Restart a container" },
    { "category": "docker", "template": "docker rm <container>", "description": "Remove a container" },
    { "category": "docker", "template": "docker rm -f <container>", "description": "Force-remove a running container" },
    { "category": "docker", "template": "docker exec -it <container> /bin/bash", "description": "Shell into a running container" },
    { "category": "docker", "template": "docker logs <container>", "description": "Show container logs" },
    { "category": "docker", "template": "docker logs <container> --follow --tail <lines>", "description": "Tail container logs" },
    { "category": "docker", "template": "docker cp <local-path> <container>:<path-in-container>", "description": "Copy file into a container" },
    { "category": "docker", "template": "docker cp <container>:<path-in-container> <local-path>", "description": "Copy file out of a container" },
    { "category": "docker", "template": "docker inspect <container>", "description": "Inspect container details" },
    { "category": "docker", "template": "docker stats", "description": "Live container resource usage" },
    { "category": "docker", "template": "docker system df", "description": "Show Docker disk usage" },
    { "category": "docker", "template": "docker system prune -a", "description": "Remove all unused images, containers, networks" },
    { "category": "docker", "template": "docker container prune", "description": "Remove all stopped containers" },
    { "category": "docker", "template": "docker image prune -a", "description": "Remove all unused images" },

    { "category": "docker-compose", "template": "docker-compose -f <compose-file> up -d", "description": "Start services in the background" },
    { "category": "docker-compose", "template": "docker-compose -f <compose-file> down", "description": "Stop and remove services" },
    { "category": "docker-compose", "template": "docker-compose -f <compose-file> restart <service>", "description": "Restart a specific service" },
    { "category": "docker-compose", "template": "docker-compose -f <compose-file> logs -f <service>", "description": "Tail logs of a service" },
    { "category": "docker-compose", "template": "docker-compose -f <compose-file> up -d --build", "description": "Rebuild and start services" },

    { "category": "git", "template": "git clone <repo-url>", "description": "Clone a repository" },
    { "category": "git", "template": "git clone <repo-url> -b <branch>", "description": "Clone a specific branch" },
    { "category": "git", "template": "git branch", "description": "List local branches" },
    { "category": "git", "template": "git branch -a", "description": "List all branches (including remotes)" },
    { "category": "git", "template": "git checkout -b <new-branch>", "description": "Create and switch to a new branch" },
    { "category": "git", "template": "git switch <branch>", "description": "Switch to an existing branch" },
    { "category": "git", "template": "git branch -d <branch>", "description": "Delete a local branch" },
    { "category": "git", "template": "git add <file>", "description": "Stage a file" },
    { "category": "git", "template": "git add .", "description": "Stage all changes" },
    { "category": "git", "template": "git commit -m <message>", "description": "Commit staged changes" },
    { "category": "git", "template": "git push origin <branch>", "description": "Push branch to origin" },
    { "category": "git", "template": "git push -u origin <branch>", "description": "Push and set upstream" },
    { "category": "git", "template": "git pull origin <branch>", "description": "Pull from origin" },
    { "category": "git", "template": "git fetch --all", "description": "Fetch all remotes" },
    { "category": "git", "template": "git stash", "description": "Stash uncommitted changes" },
    { "category": "git", "template": "git stash list", "description": "List stashes" },
    { "category": "git", "template": "git stash pop", "description": "Re-apply latest stash" },
    { "category": "git", "template": "git log --oneline -<count>", "description": "Compact log of last N commits" },
    { "category": "git", "template": "git log --graph --oneline --all", "description": "Visual log across all branches" },
    { "category": "git", "template": "git diff", "description": "Show unstaged changes" },
    { "category": "git", "template": "git diff --staged", "description": "Show staged changes" },
    { "category": "git", "template": "git diff <branch1>..<branch2>", "description": "Diff between two branches" },
    { "category": "git", "template": "git reset --soft HEAD~1", "description": "Undo last commit, keep changes staged" },
    { "category": "git", "template": "git reset --mixed HEAD~1", "description": "Undo last commit, unstage changes" },
    { "category": "git", "template": "git checkout -- <file>", "description": "Discard unstaged changes to a file" },
    { "category": "git", "template": "git cherry-pick <commit-hash>", "description": "Apply a commit from another branch" },
    { "category": "git", "template": "git rebase <branch>", "description": "Rebase current branch onto another" },
    { "category": "git", "template": "git rebase --abort", "description": "Abort an in-progress rebase" },
    { "category": "git", "template": "git rebase --continue", "description": "Continue rebase after resolving conflicts" },
    { "category": "git", "template": "git tag <tag-name>", "description": "Create a lightweight tag" },
    { "category": "git", "template": "git tag -a <tag-name> -m <message>", "description": "Create an annotated tag" },
    { "category": "git", "template": "git push origin <tag-name>", "description": "Push a tag" },
    { "category": "git", "template": "git push origin --tags", "description": "Push all tags" },

    { "category": "ssh", "template": "ssh <user>@<host>", "description": "SSH to a host" },
    { "category": "ssh", "template": "ssh -i <key-file> <user>@<host>", "description": "SSH using a specific key" },
    { "category": "ssh", "template": "ssh -p <port> <user>@<host>", "description": "SSH on a specific port" },
    { "category": "ssh", "template": "ssh-keygen -t ed25519 -C <email>", "description": "Generate an Ed25519 SSH key" },
    { "category": "ssh", "template": "ssh-keygen -t rsa -b 4096 -C <email>", "description": "Generate a 4096-bit RSA SSH key" },
    { "category": "ssh", "template": "ssh-copy-id <user>@<host>", "description": "Install your SSH key on a remote server" },
    { "category": "ssh", "template": "scp <local-file> <user>@<host>:<remote-path>", "description": "Copy file to remote host" },
    { "category": "ssh", "template": "scp <user>@<host>:<remote-file> <local-path>", "description": "Copy file from remote host" },
    { "category": "ssh", "template": "scp -r <local-dir> <user>@<host>:<remote-path>", "description": "Recursively copy a directory" },
    { "category": "ssh", "template": "ssh -L <local-port>:<remote-host>:<remote-port> <user>@<jump-host>", "description": "Local port forward via jump host" },
    { "category": "ssh", "template": "ssh -N -L <local-port>:<remote-host>:<remote-port> <user>@<bastion>", "description": "Background tunnel (no remote command)" },

    { "category": "linux", "template": "ls -la", "description": "List files in long form (incl. hidden)" },
    { "category": "linux", "template": "cp <source> <destination>", "description": "Copy a file" },
    { "category": "linux", "template": "cp -r <source-dir> <destination-dir>", "description": "Recursively copy a directory" },
    { "category": "linux", "template": "mv <source> <destination>", "description": "Move/rename a file" },
    { "category": "linux", "template": "rm <file>", "description": "Delete a file" },
    { "category": "linux", "template": "rm -rf <path>", "description": "Recursively delete a directory" },
    { "category": "linux", "template": "mkdir -p <path>", "description": "Create nested directories" },
    { "category": "linux", "template": "find <path> -name <pattern> -type f", "description": "Find files by name pattern" },
    { "category": "linux", "template": "find <path> -name <pattern> -mtime +<days> -delete", "description": "Delete files older than N days" },
    { "category": "linux", "template": "cat <file>", "description": "Print a file" },
    { "category": "linux", "template": "less <file>", "description": "Page through a file" },
    { "category": "linux", "template": "head -n <lines> <file>", "description": "First N lines of a file" },
    { "category": "linux", "template": "tail -n <lines> <file>", "description": "Last N lines of a file" },
    { "category": "linux", "template": "tail -f <file>", "description": "Follow appends to a file (live logs)" },
    { "category": "linux", "template": "grep -r <pattern> <path>", "description": "Recursive grep" },
    { "category": "linux", "template": "grep -rn <pattern> --include=<glob> <path>", "description": "Recursive grep with file filter" },
    { "category": "linux", "template": "df -h", "description": "Filesystem disk usage" },
    { "category": "linux", "template": "du -sh <path>", "description": "Total size of a directory" },
    { "category": "linux", "template": "du -sh <path> --max-depth=1", "description": "Top-level dir sizes" },
    { "category": "linux", "template": "ps aux | grep <process>", "description": "Find a process by name" },
    { "category": "linux", "template": "kill <pid>", "description": "Send TERM to a process" },
    { "category": "linux", "template": "kill -9 <pid>", "description": "Force-kill a process" },
    { "category": "linux", "template": "tar -czf <archive>.tar.gz <path>", "description": "Create a gzipped tarball" },
    { "category": "linux", "template": "tar -xzf <archive>.tar.gz", "description": "Extract a gzipped tarball" },
    { "category": "linux", "template": "gzip <file>", "description": "Gzip-compress a file" },
    { "category": "linux", "template": "gzip -d <file>.gz", "description": "Decompress a .gz file" },
    { "category": "linux", "template": "gzip -dc <file>.gz", "description": "Decompress to stdout" },
    { "category": "linux", "template": "ping <host>", "description": "Ping a host" },
    { "category": "linux", "template": "netstat -tlnp", "description": "List listening TCP ports (legacy)" },
    { "category": "linux", "template": "ss -tlnp", "description": "List listening TCP ports (modern)" },
    { "category": "linux", "template": "chmod +x <file>", "description": "Make a file executable" },
    { "category": "linux", "template": "chmod <mode> <file>", "description": "Set numeric permissions (e.g. 644, 755)" },
    { "category": "linux", "template": "chown <user>:<group> <file>", "description": "Change owner and group" },
    { "category": "linux", "template": "uname -a", "description": "Kernel and OS info" },
    { "category": "linux", "template": "cat /etc/os-release", "description": "OS distribution info" },
    { "category": "linux", "template": "free -h", "description": "Memory usage (human-readable)" },
    { "category": "linux", "template": "uptime", "description": "System uptime and load average" },
    { "category": "linux", "template": "nc -zv <host> <port>", "description": "Check TCP port reachability" },
    { "category": "linux", "template": "nc -zvu <host> <port>", "description": "Check UDP port reachability" },
    { "category": "linux", "template": "nc -zv -w <seconds> <host> <port>", "description": "Port check with timeout" },
    { "category": "linux", "template": "nc -zv <host> <port-range>", "description": "Check a range of ports (e.g. 9092-9094)" },
    { "category": "linux", "template": "traceroute <host>", "description": "Trace network path to host" },
    { "category": "linux", "template": "traceroute -T -p <port> <host>", "description": "TCP-based traceroute to a port" },
    { "category": "linux", "template": "traceroute -n <host>", "description": "Numeric traceroute (skip DNS)" },

    { "category": "curl", "template": "curl -s <url>", "description": "Silent HTTP GET" },
    { "category": "curl", "template": "curl -X POST <url> -H <header> -d <body>", "description": "HTTP POST with header and body" },
    { "category": "curl", "template": "curl -s -u <user>:<password> <url>", "description": "GET with basic auth" },
    { "category": "curl", "template": "curl -s -H <header> <url>", "description": "GET with a single header" },
    { "category": "curl", "template": "wget <url>", "description": "Download a file" },

    { "category": "powershell", "template": "Get-ChildItem", "description": "List files in current directory" },
    { "category": "powershell", "template": "Get-ChildItem -Path <path>", "description": "List files at a path" },
    { "category": "powershell", "template": "Copy-Item <source> <destination>", "description": "Copy a file" },
    { "category": "powershell", "template": "Copy-Item -Recurse <source-dir> <destination-dir>", "description": "Recursively copy a directory" },
    { "category": "powershell", "template": "Move-Item <source> <destination>", "description": "Move/rename a file" },
    { "category": "powershell", "template": "Remove-Item <path>", "description": "Delete a file" },
    { "category": "powershell", "template": "Remove-Item <path> -Recurse -Force", "description": "Recursively delete" },
    { "category": "powershell", "template": "Test-Path <path>", "description": "Check if a path exists" },
    { "category": "powershell", "template": "Get-Content <file>", "description": "Read a file's contents" },
    { "category": "powershell", "template": "Get-Content <file> -Tail <lines> -Wait", "description": "Tail a file (live)" },
    { "category": "powershell", "template": "Set-Location <path>", "description": "cd into a path" },
    { "category": "powershell", "template": "$env:<variable> = <value>", "description": "Set an environment variable" },
    { "category": "powershell", "template": "Get-ChildItem Env:", "description": "List environment variables" },
    { "category": "powershell", "template": "Select-String -Path <file> -Pattern <pattern>", "description": "grep equivalent" },

    { "category": "mysql", "template": "mysql -h <host> -P <port> -u <user> -p", "description": "Connect to MySQL/TiDB interactively" },
    { "category": "mysql", "template": "mysql -h <host> -P <port> -u <user> -p<password> -e <query>", "description": "Run a single query" },
    { "category": "mysql", "template": "mysql -h <host> -P <port> -u <user> -p <database>", "description": "Connect directly to a database" },
    { "category": "mysql", "template": "mysqldump -h <host> -P <port> -u <user> -p <database> > <output-file>.sql", "description": "Dump a whole database to SQL" },
    { "category": "mysql", "template": "mysqldump -h <host> -P <port> -u <user> -p --quick --skip-lock-tables --skip-add-locks --no-tablespaces <database> > <output-file>.sql", "description": "TiDB-compatible dump (no locks)" },
    { "category": "mysql", "template": "mysqldump -h <host> -P <port> -u <user> -p --quick --skip-lock-tables --skip-add-locks --no-tablespaces <database> | gzip > <output-file>.sql.gz", "description": "TiDB dump compressed with gzip" },
    { "category": "mysql", "template": "mysqldump -h <host> -P <port> -u <user> -p <database> <table> > <output-file>.sql", "description": "Dump a single table" },
    { "category": "mysql", "template": "mysql -h <host> -P <port> -u <user> -p <database> < <input-file>.sql", "description": "Restore from SQL file" },
    { "category": "mysql", "template": "gzip -dc <dump-file>.sql.gz | mysql -h <host> -P <port> -u <user> -p <database>", "description": "Restore from gzipped SQL dump" },

    { "category": "kafka", "template": "kafka-topics.sh --bootstrap-server <broker> --list", "description": "List Kafka topics" },
    { "category": "kafka", "template": "kafka-topics.sh --bootstrap-server <broker> --describe --topic <topic>", "description": "Describe a topic" },
    { "category": "kafka", "template": "kafka-topics.sh --bootstrap-server <broker> --create --topic <topic> --partitions <partitions> --replication-factor <replicas>", "description": "Create a topic" },
    { "category": "kafka", "template": "kafka-topics.sh --bootstrap-server <broker> --delete --topic <topic>", "description": "Delete a topic" },
    { "category": "kafka", "template": "kafka-console-producer.sh --bootstrap-server <broker> --topic <topic>", "description": "Start a console producer" },
    { "category": "kafka", "template": "kafka-console-consumer.sh --bootstrap-server <broker> --topic <topic> --from-beginning", "description": "Consume from beginning" },
    { "category": "kafka", "template": "kafka-console-consumer.sh --bootstrap-server <broker> --topic <topic> --group <group-id>", "description": "Consume as a consumer group" },
    { "category": "kafka", "template": "kafka-consumer-groups.sh --bootstrap-server <broker> --list", "description": "List consumer groups" },
    { "category": "kafka", "template": "kafka-consumer-groups.sh --bootstrap-server <broker> --describe --group <group-id>", "description": "Describe a consumer group" },
    { "category": "kafka", "template": "kafka-consumer-groups.sh --bootstrap-server <broker> --group <group-id> --topic <topic> --reset-offsets --to-earliest --execute", "description": "Reset offsets to earliest" },

    { "category": "keytool", "template": "keytool -list -keystore <keystore-file> -storepass <password>", "description": "List keystore entries" },
    { "category": "keytool", "template": "keytool -list -v -keystore <keystore-file> -storepass <password>", "description": "Verbose keystore listing" },
    { "category": "keytool", "template": "keytool -import -alias <alias> -file <cert-file> -keystore <keystore-file> -storepass <password>", "description": "Import a certificate" },
    { "category": "keytool", "template": "keytool -export -alias <alias> -keystore <keystore-file> -file <output-file> -storepass <password>", "description": "Export a certificate" },
    { "category": "keytool", "template": "keytool -delete -alias <alias> -keystore <keystore-file> -storepass <password>", "description": "Delete a keystore entry" },
    { "category": "keytool", "template": "keytool -genkeypair -alias <alias> -keyalg RSA -keysize 2048 -keystore <keystore-file> -storepass <password> -validity <days>", "description": "Generate a new keypair" },
    { "category": "keytool", "template": "keytool -storepasswd -keystore <keystore-file> -storepass <old-password> -new <new-password>", "description": "Change keystore password" },
    { "category": "keytool", "template": "keytool -importkeystore -srckeystore <source-p12> -srcstoretype PKCS12 -destkeystore <dest-jks> -deststoretype JKS", "description": "Convert PKCS12 to JKS" },

    { "category": "kcadm", "template": "./kcadm.sh config credentials --server <keycloak-url> --realm master --user <admin-user> --password <admin-password>", "description": "Login as Keycloak admin" },
    { "category": "kcadm", "template": "./kcadm.sh get realms", "description": "List realms" },
    { "category": "kcadm", "template": "./kcadm.sh get realms/<realm-name>", "description": "Get a realm" },
    { "category": "kcadm", "template": "./kcadm.sh create realms -s realm=<realm-name> -s enabled=true", "description": "Create a realm" },
    { "category": "kcadm", "template": "./kcadm.sh get clients -r <realm-name>", "description": "List clients in a realm" },
    { "category": "kcadm", "template": "./kcadm.sh get users -r <realm-name>", "description": "List users in a realm" },
    { "category": "kcadm", "template": "./kcadm.sh create users -r <realm-name> -s username=<username> -s email=<email> -s enabled=true", "description": "Create a user" },
    { "category": "kcadm", "template": "./kcadm.sh set-password -r <realm-name> --username <username> --new-password <password>", "description": "Set a user's password" },
    { "category": "kcadm", "template": "./kcadm.sh add-roles -r <realm-name> --uusername <username> --rolename <role>", "description": "Add a role to a user" },

    { "category": "maven", "template": "mvn clean install -DskipTests", "description": "Clean install, skip tests" },
    { "category": "maven", "template": "mvn clean install -P <profile>", "description": "Build with a Maven profile" },
    { "category": "maven", "template": "mvn spring-boot:run", "description": "Run Spring Boot app" },
    { "category": "maven", "template": "mvn spring-boot:run -Dspring-boot.run.profiles=<profile>", "description": "Run with a Spring profile" },
    { "category": "maven", "template": "mvn clean package -DskipTests", "description": "Package as JAR/WAR" },
    { "category": "maven", "template": "mvn dependency:tree", "description": "Print dependency tree" },
    { "category": "maven", "template": "mvn versions:display-dependency-updates", "description": "List available dependency updates" },

    { "category": "gradle", "template": "./gradlew build", "description": "Run a Gradle build" },
    { "category": "gradle", "template": "./gradlew build -x test", "description": "Build and skip tests" },
    { "category": "gradle", "template": "./gradlew bootRun", "description": "Run Spring Boot app" },
    { "category": "gradle", "template": "./gradlew bootRun --args=--spring.profiles.active=<profile>", "description": "Run with a Spring profile" },
    { "category": "gradle", "template": "./gradlew clean build", "description": "Clean and build" },
    { "category": "gradle", "template": "./gradlew dependencies", "description": "List dependencies" },

    { "category": "java", "template": "java -jar <jar-file>", "description": "Run an executable JAR" },
    { "category": "java", "template": "java -Xms<min-heap> -Xmx<max-heap> -jar <jar-file>", "description": "Run a JAR with heap settings" },
    { "category": "java", "template": "java -Dspring.profiles.active=<profile> -jar <jar-file>", "description": "Run a JAR with a Spring profile" },
    { "category": "java", "template": "java -Duser.timezone=<timezone> -jar <jar-file>", "description": "Run a JAR with a specific timezone" },
    { "category": "java", "template": "java -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:<debug-port> -jar <jar-file>", "description": "Run a JAR with remote debugging" },

    { "category": "harbor", "template": "curl -s -u <user>:<password> https://<harbor-url>/api/v2.0/projects", "description": "List Harbor projects" },
    { "category": "harbor", "template": "curl -s -u <user>:<password> https://<harbor-url>/api/v2.0/projects/<project>/repositories", "description": "List repositories in a project" },
    { "category": "harbor", "template": "curl -s -u <user>:<password> https://<harbor-url>/api/v2.0/projects/<project>/repositories/<repo>/artifacts", "description": "List artifacts/tags for a repo" },
    { "category": "harbor", "template": "curl -s -u <user>:<password> -X DELETE https://<harbor-url>/api/v2.0/projects/<project>/repositories/<repo>/artifacts/<digest>", "description": "Delete an artifact by digest" },
    { "category": "harbor", "template": "curl -s -u <user>:<password> -X POST https://<harbor-url>/api/v2.0/system/gc/schedule -H Content-Type:application/json -d {\"schedule\":{\"type\":\"Manual\"}}", "description": "Trigger Harbor garbage collection" },

    { "category": "containerd", "template": "sudo ctr -n k8s.io images list", "description": "List containerd images on a node" },
    { "category": "containerd", "template": "sudo ctr -n k8s.io images list | grep <image-name>", "description": "Search containerd images by name" },
    { "category": "containerd", "template": "sudo ctr -n k8s.io images export <output-tar> <full-image-reference>", "description": "Export an image to a tarball" },
    { "category": "containerd", "template": "sudo ctr -n k8s.io images import <input-tar>", "description": "Import an image tarball" },
    { "category": "containerd", "template": "sudo /var/lib/rancher/rke2/bin/ctr --address /run/k3s/containerd/containerd.sock -n k8s.io images list", "description": "List images on an RKE2 node" },
    { "category": "containerd", "template": "sudo /var/lib/rancher/rke2/bin/ctr --address /run/k3s/containerd/containerd.sock -n k8s.io images export <output-tar> <full-image-reference>", "description": "Export image from an RKE2 node" }
  ]
}
