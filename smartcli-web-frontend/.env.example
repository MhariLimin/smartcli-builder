# Local dev: leave blank — Vite proxies /api to :8080 (see vite.config).
# Production (Vercel): set to the deployed backend, e.g.
#   VITE_API_URL=https://smartcli-web-api.onrender.com/api
VITE_API_URL=
